name: docs

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/configure-pages@v5

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2

      - name: Install mdBook
        uses: peaceiris/actions-mdbook@v2
        with:
          mdbook-version: 'latest'

      - name: Build mdBook
        run: mdbook build docs --dest-dir site

      - name: Build Rust API docs
        run: cargo doc --workspace --all-features --no-deps

      - name: Assemble site
        run: |
          # Ensure root site dir exists and include mdBook output (built to docs/site)
          mkdir -p site
          cp -r docs/site/* site/

          # Copy Rust API docs under /api/rust
          mkdir -p site/api/rust
          cp -r target/doc/* site/api/rust/
          touch site/.nojekyll
          printf "docs.weaveffi.com" > site/CNAME

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: site

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    needs: build
    runs-on: ubuntu-latest
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
