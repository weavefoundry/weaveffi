[package]
name = "calculator"
version = "0.1.0"
edition.workspace = true

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
weaveffi-abi = { path = "../../crates/weaveffi-abi" }

[lints]
workspace = true
