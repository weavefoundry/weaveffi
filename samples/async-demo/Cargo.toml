[package]
name = "async-demo"
version = "0.1.0"
edition.workspace = true
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
weaveffi-abi = { path = "../../crates/weaveffi-abi" }

[lints]
workspace = true
