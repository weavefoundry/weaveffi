---
source: crates/weaveffi-cli/tests/snapshots.rs
---
// Generated by WeaveFFI [VERSION] from weaveffi.yml
// DO NOT EDIT. Your changes will be overwritten.
// To regenerate: weaveffi generate weaveffi.yml -o <out>

import CWeaveFFI
import Foundation

public enum WeaveFFIError: Error, LocalizedError {
    case error(code: Int32, message: String)
    public var errorDescription: String? {
        switch self {
        case let .error(_, message): return message
        }
    }
    public var errorCode: Int32 {
        switch self {
        case let .error(code, _): return code
        }
    }
}

@inline(__always)
func check(_ err: inout weaveffi_error) throws {
    if err.code != 0 {
        let code = err.code
        let message = err.message.flatMap { String(cString: $0) } ?? ""
        weaveffi_error_clear(&err)
        throw WeaveFFIError.error(code: code, message: message)
    }
}

@inline(__always)
func trap(_ err: inout weaveffi_error) {
    if err.code != 0 {
        let code = err.code
        let message = err.message.flatMap { String(cString: $0) } ?? ""
        weaveffi_error_clear(&err)
        fatalError("\(code): \(message)")
    }
}

@inline(__always)
func withOptionalPointer<T, R>(to value: T?, _ body: (UnsafePointer<T>?) throws -> R) rethrows -> R {
    guard let value = value else { return try body(nil) }
    return try withUnsafePointer(to: value) { try body($0) }
}

@inline(__always)
func withOptionalCString<R>(_ value: String?, _ body: (UnsafePointer<CChar>?) throws -> R) rethrows -> R {
    guard let value = value else { return try body(nil) }
    return try value.withCString { try body($0) }
}

@inline(__always)
func wvFreeArray<T>(_ ptr: UnsafeMutablePointer<T>, _ count: Int) {
    weaveffi_free_bytes(UnsafeMutableRawPointer(ptr).assumingMemoryBound(to: UInt8.self), count * MemoryLayout<T>.stride)
}

@inline(__always)
func wvFreeBox<T>(_ ptr: UnsafeMutablePointer<T>) {
    weaveffi_free_bytes(UnsafeMutableRawPointer(ptr).assumingMemoryBound(to: UInt8.self), MemoryLayout<T>.size)
}

/// Typed errors reported by the `contacts` module.
public enum ContactsError: Error, LocalizedError {
    case invalidName(message: String)
    case notFound(message: String)
    public var errorDescription: String? {
        switch self {
        case let .invalidName(message): return message
        case let .notFound(message): return message
        }
    }
    /// The numeric ABI code carried by this error.
    public var errorCode: Int32 {
        switch self {
        case .invalidName: return 1
        case .notFound: return 2
        }
    }
}

@inline(__always)
func mapContacts(code: Int32, message: String) -> Error {
    switch code {
    case 1: return ContactsError.invalidName(message: message.isEmpty ? "name must not be empty" : message)
    case 2: return ContactsError.notFound(message: message.isEmpty ? "contact not found" : message)
    default: return WeaveFFIError.error(code: code, message: message)
    }
}

@inline(__always)
func checkContacts(_ err: inout weaveffi_error) throws {
    if err.code != 0 {
        let code = err.code
        let message = err.message.flatMap { String(cString: $0) } ?? ""
        weaveffi_error_clear(&err)
        throw mapContacts(code: code, message: message)
    }
}

public enum ContactType: UInt32 {
    case personal = 0
    case work = 1
    case other = 2
}

public class Contact {
    let ptr: OpaquePointer

    init(ptr: OpaquePointer) {
        self.ptr = ptr
    }

    deinit {
        weaveffi_contacts_Contact_destroy(ptr)
    }

    public var id: Int64 {
        return weaveffi_contacts_Contact_get_id(ptr)
    }

    public var first_name: String {
        let raw = weaveffi_contacts_Contact_get_first_name(ptr)
        guard let raw = raw else { return "" }
        defer { weaveffi_free_string(raw) }
        return String(cString: raw)
    }

    public var last_name: String {
        let raw = weaveffi_contacts_Contact_get_last_name(ptr)
        guard let raw = raw else { return "" }
        defer { weaveffi_free_string(raw) }
        return String(cString: raw)
    }

    public var email: String? {
        let p = weaveffi_contacts_Contact_get_email(ptr)
        guard let p = p else { return nil }
        defer { weaveffi_free_string(p) }
        return String(cString: p)
    }

    public var contact_type: ContactType {
        return ContactType(rawValue: weaveffi_contacts_Contact_get_contact_type(ptr).rawValue)!
    }
}

/// An in-memory address book owning its contacts
public final class ContactBook {
    let ptr: OpaquePointer

    init(ptr: OpaquePointer) {
        self.ptr = ptr
    }

    deinit {
        weaveffi_contacts_ContactBook_destroy(ptr)
    }
    public init() {
        var err = weaveffi_error(code: 0, message: nil)
        let rv = weaveffi_contacts_ContactBook_new(&err)
        trap(&err)
        guard let rv = rv else { fatalError("-1: null pointer") }
        self.ptr = rv
    }
    public func add(firstName: String, lastName: String, email: String?, contactType: ContactType) throws -> Contact {
        var err = weaveffi_error(code: 0, message: nil)
        let result: OpaquePointer? = firstName.withCString { firstName_ptr in
            return lastName.withCString { lastName_ptr in
                return withOptionalCString(email) { email_ptr in
                        return weaveffi_contacts_ContactBook_add(ptr, firstName_ptr, lastName_ptr, email_ptr, weaveffi_contacts_ContactType(contactType.rawValue), &err)
                }
            }
        }
        try checkContacts(&err)
        guard let result = result else { throw WeaveFFIError.error(code: -1, message: "null pointer") }
        return Contact(ptr: result)
    }
    public func get(id: Int64) throws -> Contact {
        var err = weaveffi_error(code: 0, message: nil)
        let rv = weaveffi_contacts_ContactBook_get(ptr, id, &err)
        try checkContacts(&err)
        guard let rv = rv else { throw WeaveFFIError.error(code: -1, message: "null pointer") }
        return Contact(ptr: rv)
    }
    public func list() -> [Contact] {
        var err = weaveffi_error(code: 0, message: nil)
        var outLen: Int = 0
        let rv = weaveffi_contacts_ContactBook_list(ptr, &outLen, &err)
        trap(&err)
        guard let rv = rv else { return [] }
        defer { wvFreeArray(rv, outLen) }
        return (0..<outLen).map { Contact(ptr: rv[$0]!) }
    }
    public func remove(id: Int64) -> Bool {
        var err = weaveffi_error(code: 0, message: nil)
        let rv = weaveffi_contacts_ContactBook_remove(ptr, id, &err)
        trap(&err)
        return rv
    }
    public func count() -> Int32 {
        var err = weaveffi_error(code: 0, message: nil)
        let rv = weaveffi_contacts_ContactBook_count(ptr, &err)
        trap(&err)
        return rv
    }
}

public enum Contacts {
}

// END WeaveFFI.swift
