---
source: crates/weaveffi-cli/tests/snapshots.rs
---
<!-- Generated by WeaveFFI [VERSION] from weaveffi.yml -->
<!-- DO NOT EDIT. Your changes will be overwritten. -->
<!-- To regenerate: weaveffi generate weaveffi.yml -o <out> -->

# WeaveFFI Wasm (experimental)

This folder contains a minimal stub to help you load a `wasm32-unknown-unknown` build of your WeaveFFI library.

Build (example):

```bash
cargo build --target wasm32-unknown-unknown --release
```

Then serve the `.wasm` and use `weaveffi_wasm.js` to load it.

## Complex Type Handling

Wasm only supports numeric types natively (`i32`, `i64`, `f32`, `f64`). Complex types are encoded at the boundary as follows:

### Structs

Structs are passed as **opaque handles** (`i64` pointers into linear memory). The host cannot inspect struct fields directly; use the generated accessor functions (`weaveffi_{module}_{struct}_get_{field}`) to read/write fields.

### Enums

Enums are passed as **`i32` values** corresponding to the variant's integer discriminant.

### Optionals

Optional values use **`0` / `null`** to represent the absent case. For numeric optionals, a separate `_is_present` flag (`i32`: 0 or 1) is used. For handle-typed optionals, a null pointer (`0`) signals absence.

### Lists

Lists are passed as a **pointer + length** pair (`i32` pointer, `i32` length) referencing a contiguous region in linear memory. The caller is responsible for allocating and freeing the backing memory.

### Error Handling

The generated JS wrappers automatically handle errors by passing an error
pointer as the last argument to each Wasm function. Your Wasm module must
export the following functions:

- `weaveffi_alloc(size: i32) -> i32`: allocate `size` bytes in linear memory
- `weaveffi_error_clear(err_ptr: i32)`: clear and free error resources

Wrappers of functions declared `throws` raise the declaring module's typed
error class (a `WeaveFFIError` subclass with a per-code subclass, such as
`KeyNotFound`); every other wrapper raises the generic `WeaveFFIError` only
for producer panics and marshalling failures.

## API Reference

### Module: `contacts`

#### Error Domain: `ContactsError`

Throwing wrappers in this module raise `ContactsError` (a `WeaveFFIError` subclass); each code below is its own subclass carrying the stable `code`.

| Class | Code | Default Message |
|-------|------|-----------------|
| `InvalidName` | `1` | name must not be empty |
| `NotFound` | `2` | contact not found |

#### Interfaces

##### `ContactBook`

An in-memory address book owning its contacts

Passed as an **opaque handle** (`i64`), wrapped by a JS class. Constructors return an owned handle; methods pass the handle as the implicit leading `self` argument; `free()` releases the handle via the destroy symbol.

##### `weaveffi_contacts_ContactBook_new`

`weaveffi_contacts_ContactBook_new() -> i64`

| Param | API Type | Wasm | Notes |
|-------|----------|------|-------|
| _returns_ | `ContactBook` | `i64` | opaque handle in linear memory |

##### `weaveffi_contacts_ContactBook_add`

`weaveffi_contacts_ContactBook_add(first_name: i32, i32, last_name: i32, i32, email: i32, i32, contact_type: i32) -> i64`

| Param | API Type | Wasm | Notes |
|-------|----------|------|-------|
| `first_name` | `string` | `i32, i32` | ptr + len in linear memory |
| `last_name` | `string` | `i32, i32` | ptr + len in linear memory |
| `email` | `string?` | `i32, i32` | is_present flag + value |
| `contact_type` | `ContactType` | `i32` | variant discriminant |
| _returns_ | `Contact` | `i64` | opaque handle in linear memory |

##### `weaveffi_contacts_ContactBook_get`

`weaveffi_contacts_ContactBook_get(id: i64) -> i64`

| Param | API Type | Wasm | Notes |
|-------|----------|------|-------|
| `id` | `i64` | `i64` | native Wasm i64 |
| _returns_ | `Contact` | `i64` | opaque handle in linear memory |

##### `weaveffi_contacts_ContactBook_list`

`weaveffi_contacts_ContactBook_list() -> i32, i32`

| Param | API Type | Wasm | Notes |
|-------|----------|------|-------|
| _returns_ | `[Contact]` | `i32, i32` | ptr + len in linear memory |

##### `weaveffi_contacts_ContactBook_remove`

`weaveffi_contacts_ContactBook_remove(id: i64) -> i32`

| Param | API Type | Wasm | Notes |
|-------|----------|------|-------|
| `id` | `i64` | `i64` | native Wasm i64 |
| _returns_ | `bool` | `i32` | 0 = false, 1 = true |

##### `weaveffi_contacts_ContactBook_count`

`weaveffi_contacts_ContactBook_count() -> i32`

| Param | API Type | Wasm | Notes |
|-------|----------|------|-------|
| _returns_ | `i32` | `i32` | native Wasm i32 |

##### `weaveffi_contacts_ContactBook_destroy`

Releases the object reference. Called by the wrapper's `free()`.

#### Structs

##### `Contact`

Passed as opaque handle (`i64`).

| Accessor | Wasm Return |
|----------|-------------|
| `weaveffi_contacts_Contact_get_id` | `i64` |
| `weaveffi_contacts_Contact_get_first_name` | `i32, i32` |
| `weaveffi_contacts_Contact_get_last_name` | `i32, i32` |
| `weaveffi_contacts_Contact_get_email` | `i32, i32` |
| `weaveffi_contacts_Contact_get_contact_type` | `i32` |

#### Enums

##### `ContactType`

Passed as `i32` discriminant.

| Variant | Value |
|---------|-------|
| `Personal` | `0` |
| `Work` | `1` |
| `Other` | `2` |

<!-- END README.md -->
